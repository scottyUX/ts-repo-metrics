---
description: How to deploy the Next.js dashboard (Railway)
globs: apps/dashboard/**/*
alwaysApply: false
---

# Dashboard Deployment (Railway)

Analyze requires **git**. Do not deploy to Vercel — the zipball fallback is retired.

Full details: [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md).

## Build order

Dashboard `npm run build` already runs:

1. `build:engine` — `cd ../.. && cd packages/engine && npm run build`
2. `next build`

Do not change the build command to skip the engine build; the API route depends on `@repo-metrics/engine` (built `dist/`).
