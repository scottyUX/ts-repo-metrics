# Cursor log fixtures (SIP Sprint 1 — Objective 4)

Sanitized Cursor JSONL for SIP-1.4 lives in **[data/samples/cursor/](../../data/samples/cursor/)** (committed on `main`).

This folder is a legacy pointer; use `data/samples/cursor/sample-session.jsonl` for parser development and tests.

Students work in a local clone of [scottyUX/agent_stats](https://github.com/scottyUX/agent_stats) inside `agent_stats/`. If that folder is missing, follow [research/AGENT_STATS_SETUP.md](../../AGENT_STATS_SETUP.md) first.
