{
  "name": "repo-metrics",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "supabase:push": "supabase db push",
    "supabase:link": "supabase link",
    "dashboard": "cd apps/dashboard && npm run dev",
    "dashboard:build": "cd packages/engine && npm run build && cd ../.. && cd apps/dashboard && npm run build",
    "build:engine": "cd packages/engine && npm run build",
    "dev": "npm run build:engine && tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "build:engine": "cd packages/engine && npm run build",
    "postinstall": "npm run build:engine",
    "start": "node dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "workspaces": [
    "packages/engine",
    "apps/dashboard"
  ],
  "dependencies": {
    "@repo-metrics/engine": "file:packages/engine"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}